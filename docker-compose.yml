services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - '8001:8001'
    restart: unless-stopped
    environment:
      - NODE_OPTIONS=--max-old-space-size=256
    mem_limit: 480m
